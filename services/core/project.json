{
  "name": "coreservice",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/core/src",
  "projectType": "application",
  "tags": [
    "type:service",
    "scope:api"
  ],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun --watch --no-clear-screen src/index.ts",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun build ./src/index.ts --outdir ../../dist/{projectRoot} --target bun",
        "cwd": "{projectRoot}"
      }
    },
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "drizzle-kit generate &&drizzle-kit migrate",
        "cwd": "{projectRoot}"
      }
    },
    "seed":{
      "executor": "nx:run-commands",
      "options": {
        "command": "bun src/db/seed.ts",
        "cwd": "{projectRoot}"
      }
    },
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker compose -f ../../docker/docker-compose.dev.yml up --build -d core",
        "cwd": "{projectRoot}"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "gcloud run deploy credopass-api --source ../.. --region us-central1",
        "cwd": "{projectRoot}"
      }
    },
    "studio": {
      "executor": "nx:run-commands",
      "options": {
        "command": "drizzle-kit studio",
        "cwd": "{projectRoot}"
      }
    }
  }
}