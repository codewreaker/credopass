# Environment Configuration Example
# Copy this file to .env and adjust values as needed

# Node Environment
NODE_ENV=development

# API Server Port
# Used by both server.ts and apps/api/src/index.ts
# Default: 3000
# Make sure to use different ports if running both servers simultaneously
PORT=3000

# Frontend URL for CORS
# Default: http://localhost:5173
FRONTEND_URL=http://localhost:5173

# API Port for Vite proxy (apps/web/vite.config.ts)
# Should match the PORT value above
API_PORT=3000

# Throttle delay for API testing (in milliseconds)
# Set to 0 to disable throttling
THROTTLE_DELAY=0

# Database Configuration
# Add your database connection strings here
# DATABASE_URL=
