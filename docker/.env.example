# =============================================================================
# Environment Variables Template
# =============================================================================
# Copy this file to create environment-specific configs:
#   cp .env.example .env          # Development
#   cp .env.example .env.prod     # Production testing
#
# NEVER commit actual .env files to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=credopass_db

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=8080

# -----------------------------------------------------------------------------
# Database Connection (constructed from above)
# -----------------------------------------------------------------------------
# For local development (connecting to Docker postgres):
# DATABASE_URL=postgresql://postgres:your_secure_password_here@localhost:5432/credopass_db
#
# For Docker Compose (service name as host):
# DATABASE_URL=postgresql://postgres:your_secure_password_here@postgres:5432/credopass_db
#
# For Cloud Run (Cloud SQL):
# DATABASE_URL=postgresql://credopass:<password>@/<dbname>?host=/cloudsql/<connection-name>
